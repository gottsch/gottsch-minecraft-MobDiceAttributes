{
	"_comment": "Example difficulty rule. Copy into any datapack to scale rolling per world difficulty. 'enable' defaults to true, 'multiplier' defaults to 1.0. Omit either field to use the default. Multipliers stack multiplicatively with any active dimension rule. Valid targets: peaceful, easy, normal, hard.",
	"targets": [
		"hard"
	],
	"enable": true,
	"multiplier": 1.0
}
