modLoader="javafml" #mandatory
loaderVersion="[43,)" #mandatory 
license="LGPL v3"
issueTrackerURL="https://github.com/gottsch/gottsch-minecraft-MobDiceAttributes/issues"
[[mods]] #mandatory
# The modid of the mod
modId="mobdiceattribs" #mandatory
version="1.0.0" #mandatory
 # A display name for the mod
displayName="Mob Dice Attributes" #mandatory
updateJSONURL="https://raw.githubusercontent.com/gottsch/gottsch-minecraft-MobDiceAttributes/1.19.3-main/update.json"
logoFile="mobdiceattribs.png" #optional
# A text field displayed in the mod UI
credits="" #optional
# A text field displayed in the mod UI
authors="gottsch" #optional

# The description text for the mod (multi line!) (#mandatory)
description='''
'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.mobdiceattribs]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[43,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="SERVER"
# Here's another dependency
[[dependencies.mobdiceattribs]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.19.2,1.20)"
    ordering="NONE"
    side="SERVER"
[[dependencies.mageflame]]
    modId="gottschcore"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[2.0.0,]"
    ordering="BEFORE"
    side="SERVER"